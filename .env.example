# Root-level shared environment hints
# Copy values to backend/.env for API runtime

FLASK_ENV=development
SECRET_KEY=change-this-secret
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/hireready
CORS_ORIGINS=*
